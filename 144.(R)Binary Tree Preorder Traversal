/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    vector<int> preorderTraversal(TreeNode* root) {
        vector<int> path;
		getPreorder(path, root);
		return path;
    }
	private:
	void getPreorder(vector<int> &path,TreeNode* cur)
	{
		if(!cur)
			return;
		path.push_back(cur->val);
		if(cur->left)
			getPreorder(path, cur->left);
		if(cur->right)
			getPreorder(path, cur->right);
	}
};
